# Flower Shop Project

## Running
1. Run Maven install **in the parent project**.
2. Run the server using the exec:java goal in the server module. (Configs are specified in screenshot)
3. Run the client using the javafx:run goal in the client module. (Configs are specified in screenshot)
